% to run --> $ xelatex [filename].tex
\documentclass[tikz,convert={outfile=\jobname.svg}]{standalone}
%\usetikzlibrary{...}% tikz package already loaded by 'tikz' option
\usetikzlibrary{mindmap,trees}

\usepackage{fontspec}
\defaultfontfeatures{Mapping=tex-text,Scale=3}
\setmainfont{M+ 2m}

\colorlet{cyan}[rgb]{cyan}
\colorlet{magenta}[rgb]{magenta}
\colorlet{teal}[rgb]{teal}
\colorlet{olive}[rgb]{olive}


\begin{document}
\begin{tikzpicture}[mindmap,
                    grow cyclic,
                    every node/.style=concept,
                    concept color=orange,
                    text=white,
                    level 1/.append style={level distance=5cm,%%%%%%%%%%%%%%%%% child nodes
                                           font=\Huge,%%%%%%%%%%%%%%%%%%%%%%%%% huge font for level 1 child nodes
                                           sibling angle=51},
                    level 2/.append style={level distance=3cm,%%%%%%%%%%%%%%%%% grandchildchild nodes
                                           sibling angle=45}
                    ]
    \node[concept color=orange]{\large\bfseries{PySAL}}%%%%%%%%%%%%%%%%%%%%%%%%%%% huge, bold font for root node
	child [concept color=olive]{ node {}%%%%%%%%%%%%%%%%%%%% adjust here for child node color blends
		child { node { }}%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% three grandchildren for each child
		child { node { }}
		child { node { }}
	}
    child [concept color=magenta]{ node {}%%%%%%%%%%%%%%%%%% adjust here for child node color blends
		child { node { }}
		child { node { }}
		child { node { }}
	}
	child [concept color=teal]{ node {}%%%%%%%%%%%%%%%%%%%%%%% adjust here for child node color blends
		child { node { }}
		child { node { }}
		child { node { }}
	}
	child [concept color=violet!20!red!60]{ node {}%%%%%%%%%%%%%%%%%%% adjust here for child node color blends
		child { node { }}
		child { node { }}
		child { node { }}
	}
	child [concept color=violet!10!blue!80]{ node {}%%%%%%%%%%%%%%%%%%%% adjust here for child node color blends
		child { node { }}
		child { node { }}
		child { node { }}
	}
	child [concept color=black!5!cyan]{ node {}%%%%%%%%%%%%%%%%%%%%%%%%%% adjust here for child node color blends
		child { node { }}
		child { node { }}
		child { node { }}
	}
	child [concept color=black!1!red]{ node {}%%%%%%%%%%%%%%%%%%%%%%%%% adjust here for child node color blends
		child { node { }}
		child { node { }}
		child { node { }}
	}
    	;
\end{tikzpicture}
\end{document}